name: Keep Alive

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *" # Executa a cada 10 minutos

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Keep-Alive Request
        run: |
          curl -X GET https://meu-app.onrender.com
