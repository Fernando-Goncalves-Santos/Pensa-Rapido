name: Keep Alive

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */5 * ? * *" # Executa a cada 10 minutos

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Keep-Alive Request
        run: |
          curl -X GET https://pensa-rapido.onrender.com
